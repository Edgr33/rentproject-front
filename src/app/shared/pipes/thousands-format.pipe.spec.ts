import { ThousandsFormatPipe } from './thousands-format.pipe';

describe('ThousandsFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new ThousandsFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
